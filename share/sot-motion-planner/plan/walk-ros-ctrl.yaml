# Walk from ROS

duration: 30

motion:
  - walk-ros:
      interval: [0, 30]
      ros-parameter: /halfsteps_pattern_generator/walk_movement
      ros-left-ankle: /plan_left_ankle

control:
  - control-ros:
      weight: 1.
      topic: /error
      signal: error
